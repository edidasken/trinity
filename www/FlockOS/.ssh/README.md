# Repo-local SSH Metadata

This folder intentionally contains only the public SSH configuration reference.
The actual private key must remain in `~/.ssh/id_ed25519_herald_20260524_084750`.

GitHub should authenticate as `edidasken`.